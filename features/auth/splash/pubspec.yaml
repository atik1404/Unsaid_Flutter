name: splash
description: "A new Flutter package project."
version: 0.0.1
resolution: workspace

environment:
  sdk: ^3.11.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  navigation: ^0.0.1
  designsystem: ^0.0.1
  ui: ^0.0.1

  go_router: ^17.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
