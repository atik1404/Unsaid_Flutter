name: login
description: "A new Flutter package project."
version: 0.0.1
resolution: workspace

melos:
  tags:
    - feature

environment:
  sdk: ^3.11.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  code_analyzer: ^0.0.1
  navigation: ^0.0.1

  go_router: ^17.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
