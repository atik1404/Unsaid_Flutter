# NOTE: gradle-wrapper.jar is intentionally committed (not ignored) so that
# CI's `gradle/actions/wrapper-validation` can verify it, and so builds are
# reproducible. The Flutter template ignores it by default — we override that.
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
