name: di

publish_to: none
description: ""
version: 0.0.1
resolution: workspace

# Note: The environment configuration and dependency versions are managed by Melos.
#
# Do not edit them manually.
#
# Steps to update dependencies:
#   1. Modify the version in the pubspec.yaml file.
#   2. Run `melos bootstrap` to apply changes.
#
# Steps to add a new dependency:
#   1. Add the dependency to this list.
#   2. Add it to the pubspec.yaml file for future updates.

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

  code_analyzer: ^0.0.1

  data: ^0.0.1
  domain: ^0.0.1
  secured: ^0.0.1
  sharedpref: ^0.0.1

  # Navigation & feature route registration
  navigation: ^0.0.1
  splash: ^0.0.1
  login: ^0.0.1

  dio: ^5.9.2
  go_router: ^17.2.0

  get_it: ^9.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  dart_code_linter: ^4.0.2
